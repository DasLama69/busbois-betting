// Bus Bois Betting — Supabase Configuration
// Copy this file to config.js (already in .gitignore) and fill in your credentials.
// index.html loads config.js automatically before the app starts.
window.BBB_CONFIG = {
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'your-anon-key-here'
};
